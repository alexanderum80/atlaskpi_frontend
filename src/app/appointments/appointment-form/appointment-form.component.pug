div(layout="column")
    //- source
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="source", label="source")        
    //- fullname
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="fullname", label="fullname")        
    //- reason
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="reason", label="reason")        
    //- from
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="from", label="from")        
    //- to
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="to", label="to")        

    