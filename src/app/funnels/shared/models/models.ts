export interface IKpiDateRangePickerDateRange {
    predefinedDateRange: string;
    from: string;
    to: string;
}

export interface IClickedStageInfo {
    funnelId: string;
    stageId: string;
    stageName: string;
}

