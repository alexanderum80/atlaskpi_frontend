query FindTargetById($id: String) {
    findTargetById(id: $id) {
        _id
        name
        datepicker
        vary
        amount
        amountBy
        period
        owner
        target
        visible
        notify {
            users
            notification
        }
        chart
        stackName
        nonStackName
    }
}