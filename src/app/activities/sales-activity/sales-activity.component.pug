.activity(flex, layout="row")
    .activity-icon
        i.zmdi.zmdi-money
    .flex(layout="column")
        .activity-content.flex(layout="row", layout-padding)
            .flex(layout="column")
                .activity-header.m-b-10 SALES
                .activity-body Your total sales yesterday were
                    strong {{ sales[0].total | currency }}
          
            div.activity-detail-icon(layout="row", layout-align="end end")
                i.zmdi.zmdi-eye((click)="showDetails()")
    
        div.users-activity-border(*ngIf="viewDetails", layout="row")
            div(flex)
                div This data comes from 
                    b(*ngFor="let source of sales[0].revenueSources; index as i")
                        strong {{ source.source }} 
                        | with an amount of 
                        strong {{ source.amount | currency }}
                        b(*ngIf="i + 2 < countDataSource") , 
                        b(*ngIf="countDataSource - (i + 1) === 1") , and 
                    | .This data was collected on 
                    strong {{ startDayDate | date:'MM/dd/y' }} 
                    | at 
                    strong {{ startDayDate | date:'hh:mm aa' }}


            div(flex-initial)
                div(layout="row", layout-align="end start")
                    i.zmdi.zmdi-close((click)="hideDetails()")