div(layout="column")
    //- name
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="name", label="Name")        

    //- manager
    .m-t-15(flex)
        bw-text-box([fg]="vm.fg", field="manager", label="Manager Name")
