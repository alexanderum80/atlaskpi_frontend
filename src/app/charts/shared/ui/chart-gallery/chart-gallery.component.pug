div(layout="column")
    .flex
        div(*ngFor="let category of chartCategories")
            .text-center.separator {{ category }}
            div(layout="row", layout-wrap, layout-align="middle center")
                kpi-chart-gallery-item.chart-item(*ngFor="let chart of charts[category]", [item]="chart", [type]="category", [width]="100")